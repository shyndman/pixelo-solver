import pixelo._

object scratch {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(112); 
  Solver.possibilities(new LineDesc(Row, 0, Array(10, 5))).foreach(println(_))}
  
}
